diff --git a/src/contents/inject-main-world.ts b/src/contents/inject-main-world.ts
index b0b50d1..5db2561 100644
--- a/src/contents/inject-main-world.ts
+++ b/src/contents/inject-main-world.ts
@@ -743,6 +743,21 @@ function cueClock(graph: PlaybackGraph | null): CueClockInput {
   };
 }
 
+// Decoding the held Opus into deck-ready frames is the last step before a fade,
+// and while it runs the cue can do nothing but wait. Upstream asks for it
+// DECODE_LEAD_SECONDS before the fade would start, which is generous in a browser
+// tab and not here, where separating the track after next is spending the same
+// seconds: a decode that overruns costs the entire transition, silently, one second
+// from the end. Nothing about the request has to wait for the clock — the Opus is
+// held the moment the stems are staged — so it goes out then, and the only price is
+// holding the decoded frames for the rest of the track instead of the last few
+// seconds of it.
+function requestStagedDecode(): void {
+  const videoId = staging.beginStemsDecode();
+  if (videoId === null) return;
+  postToWindow({ type: "blk-request-staged-deck", videoId });
+}
+
 function runTransitionCue(graph: PlaybackGraph): boolean {
   if (crossfadeSeconds <= 0) return false;
   const state = graph.describe();
@@ -763,9 +778,9 @@ function runTransitionCue(graph: PlaybackGraph): boolean {
     return false;
   }
   if (cue.kind === "decode") {
-    const videoId = staging.beginStemsDecode();
-    if (videoId === null) return false;
-    postToWindow({ type: "blk-request-staged-deck", videoId });
+    // Normally already asked for, the moment the stems were staged. This is what
+    // answers the case where that request could not be made yet.
+    requestStagedDecode();
     return false;
   }
 
@@ -1008,6 +1023,7 @@ window.addEventListener("message", event => {
     cancelMixRequest();
     staging.offerStems(data.videoId);
     logger.log(`${data.videoId} is staged, a transition into it is possible`);
+    requestStagedDecode();
     return;
   }
 
diff --git a/src/orchestrator/karaoke-pipeline.ts b/src/orchestrator/karaoke-pipeline.ts
index 0b1d77a..99aab47 100644
--- a/src/orchestrator/karaoke-pipeline.ts
+++ b/src/orchestrator/karaoke-pipeline.ts
@@ -75,6 +75,9 @@ import {
 const CAPTURE_REQUEST_TIMEOUT_MS = 8000;
 const ACQUISITION_WATCHDOG_MS = 8000;
 const CROSSFADE_ARM_GRACE_SECONDS = 6;
+// Long enough that a decode taking this much of a track cannot make its own cue,
+// short enough to still be inside the track it belongs to.
+const STAGED_DECODE_WARN_MS = 20_000;
 
 const logger = createLogger("orchestrator");
 
@@ -540,8 +543,22 @@ function createKaraokePipeline(options: KaraokePipelineOptions): KaraokePipeline
       return;
     }
 
+    // This decode is the one step of a transition with no deadline of its own, and
+    // when it overruns the only trace is the page world skipping the fade for a
+    // track that "was still decoding". So it is timed, and one that is still running
+    // long after the fade should have started says so itself rather than leaving the
+    // reason to be inferred from a gap in the log.
+    const startedAtMs = Date.now();
+    const elapsed = () => `${((Date.now() - startedAtMs) / 1000).toFixed(1)} s`;
+    const watchdog = setTimeout(() => {
+      logger.warn(
+        `the staged stems for ${videoId} have been decoding for ${elapsed()} and have not landed yet; a transition into it will be skipped if the track ends first`
+      );
+    }, STAGED_DECODE_WARN_MS);
+
     Promise.all([decodeOpusToPcm(held.vocals), decodeOpusToPcm(held.instrumental)])
       .then(([vocals, instrumental]) => {
+        clearTimeout(watchdog);
         if (aheadStaging.heldVideoId !== videoId) return;
         const message: StageDeckMessage = {
           type: "blk-stage-deck",
@@ -554,10 +571,11 @@ function createKaraokePipeline(options: KaraokePipelineOptions): KaraokePipeline
         const transfer = [...vocals.channels, ...instrumental.channels].map(channel => channel.buffer);
         postToPageWorld(message, transfer);
         aheadStaging.releaseHeld(videoId);
-        log(`handed ${videoId} to the idle deck, ${frames} frames`);
+        log(`handed ${videoId} to the idle deck, ${frames} frames, decoded in ${elapsed()}`);
       })
       .catch(error => {
-        logError(`failed to decode the staged stems for ${videoId}`, error);
+        clearTimeout(watchdog);
+        logError(`failed to decode the staged stems for ${videoId} after ${elapsed()}`, error);
         resetStaging();
       });
   }
